{% block html_head_container %}

{% include 'html-header.twig' %}
	{% block head %}
	{% endblock %}
	</head>
{% endblock %}

  <body class="{{body_class}}" data-template="base.twig">
    <header class="main-menu menu-visible u-full-width">
      {% block header %}
      <a href="#" class="menu-opener"><span class="hamburger"></span> Menu</a>
      <div class="container">
        <a href="{{site.url}}" class="menu-logo img-replace">
          <h1>{{site.name}}</h1>
        </a>
        {% include "menu.twig" with {'menu': menu.get_items} %}
      </div>
      {% endblock %}
    </header>

		<section role="main" class="main-content">
			{% block content %}
				Sorry, no content
			{% endblock %}
		</section>

		{% block footer %}
			{{ function('wp_footer') }}
		{% endblock %}
	</body>
</html>
